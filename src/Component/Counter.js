// import React from "react";
// import { connect } from "react-redux";
// import { incrementCount } from "../actions/Actions";
// import { decrementCount } from "../actions/Actions";

// function Counter(props) {
//   console.log(props);
//   return (
//     <div>
//       <h1>Counter:{props.count}</h1>
//       <button onClick={props.incrementCount}>IncrementCounter</button>
//       <button onClick={props.decrementCount}>DecrementCounter</button>
//     </div>
//   );
// }

// const mapStateToProps = (state) => {
//   return {
//     count: state.count,
//   };
// };

// const mapDispatchToProps = (dispatch) => {
//   return {
//     incrementCount: () => dispatch(incrementCount()),
//     decrementCount: () => dispatch(decrementCount()),
//   };
// };

// export default connect(mapStateToProps, mapDispatchToProps)(Counter);
